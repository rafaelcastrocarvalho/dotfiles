# Linked to ~/.config/git/config by install/40-link.sh.
# Identity lives in config.local, which is generated by install/60-identity.sh
# and never tracked.

[core]
	editor = nvim

[init]
	defaultBranch = main

[merge]
	tool = nvimdiff

[include]
	path = config.local
