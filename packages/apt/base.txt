# Packages installed by install/10-packages.sh on Debian/Ubuntu, one per line.
#
# This list is only reached inside containers, so it is deliberately leaner
# than the pacman one: no docker, no postgresql server, nothing a devcontainer
# gets from its own image or from the host.
#
# Names differ from Arch where Debian says so -- that is why the lists are
# separate files rather than one list plus a translation table.

build-essential
git
curl
ca-certificates

zsh
bash-completion

fzf
ripgrep
fd-find

jq
unzip
tmux

# Debian stable ships neovim 0.10, and this config needs 0.11 or newer
# (vim.hl.on_yank). install/15-nvim-release.sh replaces it with the official
# build; it is listed here only so a too-old nvim is never simply missing.
neovim
